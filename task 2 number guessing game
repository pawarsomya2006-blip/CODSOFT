#include <iostream>
#include<cstdlib>
#include<ctime>
using namespace std;

int main() {
    srand(time(0));
    int number=rand()%100+1;
    int userGuess =0;
    int attmpts = 0;
    cout<<"Wellcom number guessing game!:"<<endl;
    cout<<"i have choose a number between 1 and 100. try to guess!:"<<endl;
    do{
        cout<<"Enter your guess:";
        cin>>userGuess;
    
      if(userGuess>number){
          cout<<"Too high"<<endl;
      }
      else if (userGuess<number){
          cout<<"Too low"<<endl;
      }
      else {
          cout<<"Correct!"<<endl;
      }
  }while(userGuess!=number);
    
    return 0;
}
